/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    output: 'export',
    distDir: 'dist',
    // basePath: '/game-relax-math-ninja'
};

export default nextConfig;
